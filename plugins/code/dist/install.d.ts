/**
 * Check if honcho hooks are installed in Claude settings.
 */
export declare function checkHooksInstalled(): boolean;
/**
 * Check if a command is available in PATH.
 */
export declare function verifyCommandAvailable(command: string): boolean;
